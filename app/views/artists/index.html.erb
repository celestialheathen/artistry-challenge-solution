<h1>See below for a list of artists</h1>

<ul>
    <% @artists.each do |artist| %>
  <li><%= link_to artist.name, artist_path(artist) %></li><br>

  <% end %>
</ul>

<%= link_to "Create a new artist", new_artist_path %>